#include <stdio.h>
int main()
{
    int length = 20;
    int width = 50;

    int area = length * width;

    printf("the area of rectagle %d * %d = %d\n", length, width, area);

    return 0;
}